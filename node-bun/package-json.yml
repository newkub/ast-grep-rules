id: package-json-name-field
message: package.json must have a 'name' field
severity: warning
language: json
files:
  - package.json
rule:
  pattern: '$_'
  not:
    has:
      kind: pair
      pattern: '"name": $VALUE'
id: no-mock
message: Avoid using MOCK in comments. Consider removing mock data or moving it to test fixtures.
severity: warning
language: typescript
rule:
  pattern: $COMMENT
  regex: '(?i)mock'
  kind: comment
